<nav class="navbar navbar-expand bg-white border-bottom">
  <div class="container-fluid">
    <%= link_to (user_signed_in? ? root_path : new_user_session_path), class: "navbar-brand p-0" do %>
      <%= image_tag "/servicemark-ai-logo-black-w7R36CSZ.png", alt: "ServiceMark AI", height: 28, class: "d-block" %>
    <% end %>

    <% if user_signed_in? %>
      <div class="ms-auto d-flex align-items-center gap-2">
        <%= link_to new_job_proposal_path,
              class: "btn btn-primary btn-sm fw-bold",
              title: "New job proposal",
              "aria-label": "New job proposal" do %>
          <span aria-hidden="true">+</span>
        <% end %>
        <div class="dropdown">
          <button class="btn btn-outline-secondary border-0" type="button" data-bs-toggle="dropdown" aria-expanded="false" aria-label="Menu">
            <span class="navbar-toggler-icon"></span>
          </button>
          <ul class="dropdown-menu dropdown-menu-end">
            <li><%= link_to "Profile", profile_path, class: "dropdown-item" %></li>
            <li><%= link_to "My organization", my_organization_path, class: "dropdown-item" %></li>
            <li><%= link_to "Change password", change_password_path, class: "dropdown-item" %></li>
            <li><hr class="dropdown-divider"></li>
            <li><%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "dropdown-item", form: { class: "m-0" } %></li>
          </ul>
        </div>
      </div>
    <% end %>
  </div>
</nav>
