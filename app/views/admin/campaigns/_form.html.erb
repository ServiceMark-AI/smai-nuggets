<%= form_with model: [:admin, campaign], local: true do |f| %>
  <% if campaign.errors.any? %>
    <div class="alert alert-danger">
      <h6 class="alert-heading mb-2"><%= pluralize(campaign.errors.count, "error") %> prevented this campaign from being saved:</h6>
      <ul class="mb-0">
        <% campaign.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= f.label :name, class: "form-label" %>
    <%= f.text_field :name, class: "form-control", autofocus: true %>
  </div>

  <div class="mb-3">
    <%= f.label :status, class: "form-label" %>
    <%= f.select :status, Campaign.statuses.keys.map { |s| [s.humanize, s] }, {}, class: "form-select" %>
  </div>

  <div class="d-flex gap-2">
    <%= f.submit class: "btn btn-primary" %>
    <%= link_to "Cancel", admin_campaigns_path, class: "btn btn-outline-secondary" %>
  </div>
<% end %>
